#ifndef OOP_UTILS_H
#define OOP_UTILS_H

#include <string>
#include <vector>
#include <sstream>
std::vector<std::string> split(std::string str, char delimiter);
std::string toString(std::pair<int, int>);

#endif //OOP_UTILS_H
